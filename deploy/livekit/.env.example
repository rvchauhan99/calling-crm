# Copy to .env and fill before docker compose up
DOMAIN=lk.example.com
SIP_DOMAIN=sip.example.com
PUBLIC_IP=0.0.0.0

# Generate with: openssl rand -hex 16  (use key / secret pair)
LIVEKIT_API_KEY=APIdevkey
LIVEKIT_API_SECRET=replace_with_long_random_secret

REDIS_URL=redis://redis:6379
